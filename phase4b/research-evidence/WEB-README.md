# Phase 4B Web Evidence

The parent runner acquired and verified the source artifacts in this directory
through bounded, public, read-only Web inspection on 2026-08-26. The Core
workers were not given this catalog or these URLs in advance; their returned
source claims are matched to the parent-verified artifacts after the worker
Return is received.

The primary sources used by the runner are:

- Python 3.12 `subprocess` documentation: `https://docs.python.org/3.12/library/subprocess.html`
- Python 3.12 `asyncio` task documentation: `https://docs.python.org/3.12/library/asyncio-task.html`
- Python 3.10 versioned `asyncio` task documentation: `https://docs.python.org/3.10/library/asyncio-task.html`
- Python 3.6 versioned `asyncio` task documentation: `https://docs.python.org/pl/3.6/library/asyncio-task.html`

This file records provenance only. It is not an AI Return, does not grant
authority, and does not substitute for the generated machine-readable source
artifacts.
